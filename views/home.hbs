{{!< layouts/main }}

<div class="page-header">
    <h1 class="page-title">Cryptocurrency Markets</h1>
    <p class="page-subtitle">Real-time crypto prices and market data</p>
</div>

<!-- Market Statistics -->
<div class="stats-grid">
    <div class="stat-card">
        <div class="stat-label">Market Cap</div>
        <div class="stat-value" id="total-market-cap">$2.1T</div>
        <div class="stat-change positive">+2.4%</div>
    </div>
    <div class="stat-card">
        <div class="stat-label">24h Volume</div>
        <div class="stat-value" id="total-volume">$89.2B</div>
        <div class="stat-change negative">-1.2%</div>
    </div>
    <div class="stat-card">
        <div class="stat-label">BTC Dominance</div>
        <div class="stat-value" id="btc-dominance">52.3%</div>
        <div class="stat-change positive">+0.8%</div>
    </div>
    <div class="stat-card">
        <div class="stat-label">Active Cryptocurrencies</div>
    <div class="stat-value" id="active-cryptos">{{#if topCryptos}}{{topCryptos.length}}{{else}}30{{/if}}</div>
        <div class="market-status bullish">
            <div class="status-dot"></div>
            Bullish
        </div>
    </div>
</div>

<!-- Cryptocurrency Table -->
<div class="card">
    <div class="card-header">
        <div class="card-title">Top Cryptocurrencies</div>
        <div class="card-subtitle">Ranked by market capitalization</div>
    </div>

    {{#if topCryptos}}
        <div class="table-container">
            <table class="table">
                <thead>
                    <tr>
                        <th>Rank</th>
                        <th>Name</th>
                        <th>Price</th>
                        <th>24h Change</th>
                        <th>Market Cap</th>
                        <th>Volume (24h)</th>
                        <th>Chart</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody id="crypto-table">
                    {{#each topCryptos}}
                        <tr data-crypto-id="{{id}}" class="fade-in">
                            <td class="rank-cell">{{market_cap_rank}}</td>
                            <td>
                                <div class="crypto-name">
                                    <img src="{{image}}" alt="{{name}}" class="crypto-icon" onerror="this.src='/images/default-coin.svg'">
                                    <div>
                                        <div class="crypto-name-text">{{name}}</div>
                                        <div class="crypto-symbol">{{symbol}}</div>
                                    </div>
                                </div>
                            </td>
                            <td class="price-cell">${{formatPrice current_price}}</td>
                            <td class="change-cell {{#if (gt price_change_percentage_24h 0)}}change-positive{{else}}change-negative{{/if}}">
                                {{#if (gt price_change_percentage_24h 0)}}+{{/if}}{{formatPrice price_change_percentage_24h}}%
                            </td>
                            <td>${{formatNumber market_cap}}</td>
                            <td>${{formatNumber total_volume}}</td>
                            <td>
                                <button class="chart-btn" data-coin-id="{{ id }}" data-coin-name="{{ name }}" data-coin-symbol="{{ symbol }}">
                                    Chart
                                </button>
                            </td>
                            <td>
                                {{#if ../user}}
                                    <form action="/crypto/buy" method="POST" class="action-form">
                                        <input type="hidden" name="coinId" value="{{id}}">
                                        <input type="hidden" name="price" value="{{current_price}}">
                                        <input type="number" 
                                               name="quantity" 
                                               placeholder="0.00" 
                                               required 
                                               min="0.000001" 
                                               step="0.000001"
                                               class="form-control quantity-input">
                                        <button type="submit" class="btn btn-success btn-sm">
                                            <i class="fas fa-shopping-cart"></i>&nbsp;Buy
                                        </button>
                                    </form>
                                {{else}}
                                    <a href="/auth/login" class="btn btn-primary btn-sm">
                                        <i class="fas fa-sign-in-alt"></i>&nbsp;Login to Trade
                                    </a>
                                {{/if}}
                            </td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    {{else}}
        <div class="empty-state">
            <div class="empty-state-icon">
                <i class="fas fa-chart-line"></i>
            </div>
            <div class="empty-state-title">Loading Market Data</div>
            <div class="empty-state-text">
                <div class="loading">
                    <div class="spinner"></div>
                    Fetching cryptocurrency prices...
                </div>
            </div>
        </div>
    {{/if}}
</div>

<!-- Live Price Updates Notice -->
{{#if user}}
<div class="card" style="margin-top: 1rem;">
    <div class="card-header">
        <div class="card-title">
            <i class="fas fa-broadcast-tower"></i>&nbsp;Live Market Data
        </div>
        <div class="card-subtitle">Real-time price updates via WebSocket connection</div>
    </div>
    <div id="connection-status" class="market-status bullish">
        <div class="status-dot"></div>
        Connected
    </div>
</div>
{{/if}}

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Add animation delay to table rows
    const rows = document.querySelectorAll('#crypto-table tr');
    rows.forEach((row, index) => {
        row.style.animationDelay = `${index * 0.05}s`;
    });

    // Update rank numbers starting from 1
    const rankCells = document.querySelectorAll('.rank-cell');
    rankCells.forEach((cell, index) => {
        cell.textContent = index + 1;
    });

    // Auto-refresh market stats every 30 seconds
    setInterval(() => {
        updateMarketStats();
    }, 30000);
});

function updateMarketStats() {
    // This would typically fetch from an API
    const stats = {
        marketCap: '$2.1T',
        volume: '$89.2B',
        btcDominance: '52.3%'
    };
    
    document.getElementById('total-market-cap').textContent = stats.marketCap;
    document.getElementById('total-volume').textContent = stats.volume;
    document.getElementById('btc-dominance').textContent = stats.btcDominance;
}
</script>
</div>

<script src="/socket.io/socket.io.js"></script>
<script>
    document.addEventListener('DOMContentLoaded', () => {
        const cryptoTableBody = document.querySelector('#crypto-table');

        const socket = io();
        socket.on('priceUpdate', (updatedPrices) => {
            if (cryptoTableBody) {
                updatedPrices.forEach(crypto => {
                    const row = cryptoTableBody.querySelector(`tr[data-crypto-id="${crypto.id}"]`);
                    if (row) {
                        const priceCell = row.querySelector('.current-price');
                        if (priceCell) {
                            priceCell.textContent = `$${formatPrice(crypto.current_price)}`;
                        }
                    }
                });
            }
        });

        function formatPrice(price) {
            if (price >= 1) return price.toFixed(2);
            return price.toFixed(8);
        }

        function formatNumber(num) {
            if (num >= 1000000) {
                return (num / 1000000).toFixed(2) + 'M';
            } else if (num >= 1000) {
                return (num / 1000).toFixed(2) + 'K';
            }
            return num;
        }
    });
</script>

<!-- Chart Modal -->
<div id="chartModal" class="modal">
    <div class="modal-content">
        <div class="modal-header">
            <h2 id="chartTitle">Loading...</h2>
            <span class="close">&times;</span>
        </div>
        <div class="modal-body">
            <div class="chart-controls">
                <button class="chart-period-btn active" data-days="1">1D</button>
                <button class="chart-period-btn" data-days="7">7D</button>
                <button class="chart-period-btn" data-days="30">30D</button>
                <button class="chart-period-btn" data-days="90">90D</button>
                <button class="chart-period-btn" data-days="365">1Y</button>
            </div>
            <div class="chart-container">
                <canvas id="priceChart"></canvas>
            </div>
            <div id="chartLoading" class="loading-spinner">Loading chart data...</div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script src="/js/crypto-charts.js"></script>